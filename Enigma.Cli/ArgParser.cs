namespace Enigma.Cli;

public class ArgParser
{
    public static readonly string[] Parameters = ["--file", "--key", "--encrypt", "--decrypt"];

    public ParsedArguments Parse(string[] args)
    {
        
    }
}